{
  "scripts": {
    "postinstall": "spago install",
    "dev": "concurrently pnpm:dev:css pnpm:dev:serve",
    "dev:css": "tailwindcss -w -i scss/tailwind.scss -o dev/index.css",
    "dev:serve": "parcel serve --target dev --dist-dir public"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.0.1",
    "@parcel/core": "^2.0.1",
    "@parcel/transformer-sass": "^2.0.1",
    "autoprefixer": "^10.4.0",
    "concurrently": "^6.5.1",
    "parcel": "^2.0.1",
    "postcss": "^8.4.5",
    "postcss-nested": "^5.0.6",
    "tailwindcss": "^3.0.7"
  },
  "targets": {
    "dev": {
      "source": "dev/index.html",
      "includeNodeModules": {
        "xhr2": false
      }
    }
  }
}
